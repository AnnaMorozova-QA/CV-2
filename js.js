/* Это объявление переменной */
const button = document.querySelector('button');

/* Обработчик на кнопке, запускает логику после клика */
button.addEventListener('click', function() {
	alert('Если сумка, то Chanel')
})